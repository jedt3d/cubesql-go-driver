# Binaries and native build outputs
*.exe
*.exe~
*.dll
*.so
*.dylib
/build/

# Go test, coverage, and profiling artifacts
*.test
*.out
coverage.*
*.coverprofile
profile.cov

# Go workspace files
go.work
go.work.sum

# Local environment and credentials
.env
.env.local

# Dependency directories are intentionally not ignored: the pinned CubeSQL C
# SDK lives under third_party/ and is reviewed source, not a Go module vendor.

# Editor/IDE
# .idea/
# .vscode/
